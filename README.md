# text2bin

Transform text into binary representation. Currently only 64-bit binary representation is supported.

## cmd
```
cargo build && cargo run "<inputfile.txt>" "<binaryoutputfile>"
```

## example
Example file input
```
5
0
1
2
3
4
```